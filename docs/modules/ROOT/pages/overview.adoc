# Overview

SBOMer system is multi-component services-based system used to generate SBOMs in various formats from various sources. System components follow link:https://en.wikipedia.org/wiki/Hexagonal_architecture_(software)[Hexagonal Architecture].

## Main purpose

The purpose of SBOMer system is to generate SBOMs in various formats from various sources (such us build artifacts, container images). 

## System Architecture

The system currently consists of multiple components, which communicate with each other using the Kafka messaging system.


The functionality of the system  can be simply described by this diagram:

[mermaid]
....
graph LR
    Input(Input:Request Event) -->|Contains Generation Details| Service[SBOMer Service]
    Service -->|Orchestrates Generation| Output(Output: SBOM)
....



However, there is much more to the system. 

We can plug in and plug out various components as we please, but generally we categorize the component into categories (these categories are not enforced in any way, they are just a way to understand the system better):

* Pre-Request event components
* Post-Request event components
** Generators
** Enhancers
* Infrastructure components




### Pre - Request event components

The entry point of the system is the Request event. This event is an entity which contains all the generation detail. 

However, since source of the details can be various, can be produced by various systems and can be producen in various formats, we can have components that serve as an adapter between the source and the Request event.

For example, we can trigger a generation by clicking a button in the UI or when a pipeline of our build system reaches a certain step. Completely customizable and adaptable, we just need to have a component that can produce the Request event from the source.


One example of such component is the link:https://github.com/sbomer-project/errata-tool-handler[Errata Tool Handler]. This component listtens to build systems and produces Request events based on the type of the build and the details of the build. It can possibly fetch additional information fromother systems to construct the Request event.


### Post - Request event components
TBD

#### Generators
TBD

#### Enhancers
TBD

### Infrastructure components
TBD




